# Migration `20220721110224-lawrdle`

This migration has been generated by MikhailKochkin at 7/21/2022, 3:02:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Lawrdle" (
    "id" TEXT NOT NULL,
    "word" TEXT NOT NULL,
    "story" TEXT NOT NULL,
    "authorId" TEXT NOT NULL,
    "active" BOOLEAN NOT NULL,

    PRIMARY KEY ("id")
)

ALTER TABLE "Lawrdle" ADD FOREIGN KEY("authorId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20220704135605-tags..20220721110224-lawrdle
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -689,9 +689,9 @@
   surname             String?
   image               String?
   favourites          String[]
   subjects            String[]
-  tags           String[]
+  tags                String[]
   permissions         Permission[]
   uniId               String?
   companyId           String?
   legalPortfolioId    String?              @unique
@@ -742,8 +742,9 @@
   testPracticeResults TestPracticeResult[]
   testResults         TestResult[]
   textEditors         TextEditor[]
   textEditorResults   TextEditorResult[]
+  lawrdles            Lawrdle[]
   co_coursePages      CoursePage[]         @relation("AuthorsCoursePage")
   new_subjects        CoursePage[]         @relation("UserSubjects")
 }
@@ -914,8 +915,17 @@
   lesson         Lesson?  @relation(fields: [lessonId], references: [id])
   user           User     @relation(fields: [userId], references: [id])
 }
+model Lawrdle {
+  id          String   @id
+  word        String
+  story       String
+  authorId    String
+  active      Boolean
+  author      User     @relation(fields: [authorId], references: [id])
+}
+
 enum CheckType {
   WORD
   IDEA
 }
```


