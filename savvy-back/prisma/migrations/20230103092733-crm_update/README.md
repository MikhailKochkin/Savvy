# Migration `20230103092733-crm_update`

This migration has been generated by MikhailKochkin at 1/3/2023, 1:27:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "BusinessClient" ADD COLUMN     "surname" TEXT,
ADD COLUMN     "country" TEXT,
ADD COLUMN     "source" TEXT,
ADD COLUMN     "communication_history" JSONB,
ADD COLUMN     "sales_cycle" JSONB
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20221209071809-course_countries..20230103092733-crm_update
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -20,20 +20,25 @@
   coursePage    CoursePage @relation(fields: [coursePageId], references: [id])
 }
 model BusinessClient {
-  id                   String   @id @default(cuid())
-  email                String
-  createdAt            DateTime @default(now())
-  updatedAt            DateTime @updatedAt
-  name                 String?
-  number               String?
-  comment              String?
-  tags                 String[]
-  communication_medium String?
-  type                 String?
-  coursePageId         String?
-  coursePage           CoursePage? @relation(fields: [coursePageId], references: [id])           
+  id                    String   @id @default(cuid())
+  name                  String?
+  surname               String?
+  email                 String
+  number                String?
+  country               String?
+  source                String?
+  comment               String?
+  tags                  String[]
+  communication_medium  String?
+  type                  String?
+  communication_history Json?
+  sales_cycle           Json?
+  coursePageId          String?
+  coursePage            CoursePage? @relation(fields: [coursePageId], references: [id])    
+  createdAt             DateTime @default(now())
+  updatedAt             DateTime @updatedAt       
 }
 model ConfUser {
   id          String   @id @default(cuid())
```


