# Migration `20230408144839-growth_area`

This migration has been generated by MikhailKochkin at 4/8/2023, 6:48:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "UserLevel" ADD COLUMN     "consumedContent" JSONB,
ADD COLUMN     "myProgress" JSONB,
ADD COLUMN     "isProgressPublic" BOOLEAN DEFAULT false

CREATE TABLE "GrowthArea" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "maxProgress" INTEGER,
    "marks" JSONB,
    "userLevelId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

ALTER TABLE "GrowthArea" ADD FOREIGN KEY("userLevelId")REFERENCES "UserLevel"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20230403125616-..20230408144839-growth_area
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -908,15 +908,28 @@
   user            User?     @relation(fields: [userId], references: [id])
 }
 model UserLevel {
-  id        String   @id @default(cuid())
-  level     Float?   @default(1)
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  user      User?
+  id                      String   @id @default(cuid())
+  level                   Float?   @default(1)
+  createdAt               DateTime @default(now())
+  updatedAt               DateTime @updatedAt
+  user                    User?
+  consumedContent         Json?
+  myProgress              Json?
+  growthAreas             GrowthArea[]
+  isProgressPublic        Boolean? @default(false)
 }
+model GrowthArea {
+  id                String          @id @default(cuid())
+  name              String
+  maxProgress       Int?
+  marks             Json?          
+  userLevel         UserLevel    @relation(fields: [userLevelId], references: [id])
+  userLevelId       String
+}
+
 model EmailReminder {
   id              String      @id @default(cuid())
   userId          String?
   user            User?     @relation(fields: [userId], references: [id])
```


