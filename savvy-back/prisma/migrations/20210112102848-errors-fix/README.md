# Migration `20210112102848-errors-fix`

This migration has been generated by MikhailKochkin at 1/12/2021, 1:28:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210112101132-first..20210112102848-errors-fix
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -272,9 +272,9 @@
   forum              Forum?               @relation(fields: [forumId], references: [id])
   user               User                 @relation(fields: [userId], references: [id])
   challengeResults   ChallengeResult[]
   constructions      Construction[]
-  constructionResult ConstructionResult[]
+  constructionResults ConstructionResult[]
   documents          Document[]
   documentResult     DocumentResult[]
   exam               Exam[]
   examResult         ExamResult[]
@@ -303,9 +303,9 @@
   progress     Int?     @default(0)
   studentId    String
   lessonId     String
   lesson       Lesson   @relation(fields: [lessonId], references: [id])
-  user         User     @relation(fields: [studentId], references: [id])
+  student      User     @relation(fields: [studentId], references: [id])
 }
 model NewTest {
   id         String       @id @default(cuid())
@@ -322,9 +322,9 @@
   userId     String
   lessonId   String?
   lesson     Lesson?      @relation(fields: [lessonId], references: [id])
   user       User         @relation(fields: [userId], references: [id])
-  testResult TestResult[]
+  testResults TestResult[]
 }
 model Note {
   id        String   @id @default(cuid())
@@ -364,9 +364,9 @@
   likes      Int?         @default(1)
   tags       String[]
   userId     String
   user       User         @relation(fields: [userId], references: [id])
-  coursePage CoursePage[] @relation("PostCoursePages")
+  coursePages CoursePage[] @relation("PostCoursePages")
 }
 model Problem {
   id            String          @id @default(cuid())
@@ -384,9 +384,9 @@
   userId        String
   lessonId      String
   lesson        Lesson          @relation(fields: [lessonId], references: [id])
   user          User            @relation(fields: [userId], references: [id])
-  problemResult ProblemResult[]
+  problemResults ProblemResult[]
 }
 model ProblemResult {
   id        String   @id @default(cuid())
@@ -419,9 +419,9 @@
   userId     String
   lessonId   String?
   lesson     Lesson?      @relation(fields: [lessonId], references: [id])
   user       User         @relation(fields: [userId], references: [id])
-  quizResult QuizResult[]
+  quizResults QuizResult[]
 }
 model QuizResult {
   id        String   @id @default(cuid())
@@ -435,9 +435,9 @@
   lessonId  String?
   quizId    String?
   lesson    Lesson?  @relation(fields: [lessonId], references: [id])
   quiz      Quiz?    @relation(fields: [quizId], references: [id])
-  user      User     @relation(fields: [studentId], references: [id])
+  student   User     @relation(fields: [studentId], references: [id])
 }
 model Rating {
   id        String   @id @default(cuid())
@@ -461,9 +461,9 @@
   userId     String
   lessonId   String?
   lesson     Lesson?      @relation(fields: [lessonId], references: [id])
   user       User         @relation(fields: [userId], references: [id])
-  shotResult ShotResult[]
+  shotResults ShotResult[]
 }
 model ShotResult {
   id        String   @id @default(cuid())
@@ -518,9 +518,9 @@
   userId           String
   lessonId         String
   lesson           Lesson             @relation(fields: [lessonId], references: [id])
   user             User               @relation(fields: [userId], references: [id])
-  textEditorResult TextEditorResult[]
+  textEditorResults TextEditorResult[]
 }
 model TextEditorResult {
   id           String      @id @default(cuid())
@@ -546,9 +546,9 @@
   updatedAt  DateTime     @updatedAt
   createdAt  DateTime     @default(now())
   paidMonths Int?         @default(0)
   coursePage CoursePage[]
-  user       User[]
+  teachers       User[]
 }
 model User {
   id                           String               @id @default(cuid())
```


