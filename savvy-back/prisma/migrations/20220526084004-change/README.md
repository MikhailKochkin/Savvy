# Migration `20220526084004-change`

This migration has been generated by MikhailKochkin at 5/26/2022, 12:40:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Chat" DROP COLUMN "hasSecret",
ADD COLUMN     "isSecret" BOOLEAN DEFAULT false

ALTER TABLE "Note" DROP COLUMN "hasSecret",
ADD COLUMN     "isSecret" BOOLEAN DEFAULT false

ALTER TABLE "Problem" DROP COLUMN "hasSecret",
ADD COLUMN     "isSecret" BOOLEAN DEFAULT false
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20220526083337-gamification..20220526084004-change
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -418,9 +418,9 @@
   link_clicks     Int?     @default(0)
   lessonID   String?  @default(cuid())
   updatedAt  DateTime @updatedAt
   createdAt  DateTime @default(now())
-  hasSecret  Boolean? @default(false)
+  isSecret  Boolean? @default(false)
   next       Json?
   userId     String?
   lessonId   String?
   chat       Boolean?
@@ -471,9 +471,9 @@
   updatedAt      DateTime        @updatedAt
   lessonID       String          @default(cuid())
   nodeType       String?
   nodeID         String?         @default(cuid())
-  hasSecret      Boolean?        @default(false)
+  isSecret      Boolean?        @default(false)
   hintsList      String[]
   solutionList   String[]
   userId         String
   lessonId       String
@@ -791,9 +791,9 @@
   messages   Json?
   link_clicks     Int?     @default(0)
   updatedAt  DateTime @updatedAt
   createdAt  DateTime @default(now())
-  hasSecret  Boolean? @default(false)
+  isSecret  Boolean? @default(false)
   userId     String
   lessonId   String
   complexity Int?     @default(1)
   lesson     Lesson   @relation(fields: [lessonId], references: [id])
```


