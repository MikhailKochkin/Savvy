export declare const command = "schema-status";
export declare const desc = "Show source and timestamp of the local schema file";
import { Context } from '../';
export declare function handler(context: Context): Promise<void>;
