export declare const command = "get-schema";
export declare const describe = "Download schema from endpoint";
export declare const builder: {
    watch: {
        alias: string;
        boolean: boolean;
        description: string;
    };
    endpoint: {
        alias: string;
        describe: string;
        type: string;
    };
};
import { Context } from '../';
export declare function handler(context: Context, argv: {
    endpoint: string;
    watch: boolean;
}): Promise<boolean>;
