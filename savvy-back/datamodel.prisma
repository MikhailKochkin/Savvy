type User {
  id: ID! @unique
  name: String!
  email: String! @unique
}

type Case {
  id: ID! @unique
  title: String!
  description: String!
  mainText: String
  issue: String
  rule: String
  analysis: String
  conclusion: String
  image: String!
  largeImage: String!
  price: Int!
  # user: User!
}