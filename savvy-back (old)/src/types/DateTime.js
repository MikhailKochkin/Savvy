// const { asNexusMethod } = require("nexus");
// const { GraphQLDateTime } = require("graphql-iso-date");
// const DateTime = asNexusMethod(GraphQLDateTime, { name: "date" });

// module.exports = {
//   DateTime,
// };
